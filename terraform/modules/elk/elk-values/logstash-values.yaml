replicaCount: 1

logstashPipeline:
  logstash.conf: |
    input { tcp { port => 5000 } }
    output { elasticsearch { hosts => ["http://elasticsearch-master:9200"] } }

resources:
  requests:
    cpu: 100m
    memory: 256Mi
  limits:
    cpu: 500m
    memory: 1Gi
